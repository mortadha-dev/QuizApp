<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tun.dev.comeon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComeOn">
        <activity
            android:name=".activities.ResultActivity"
            android:exported="true" />
        <activity android:name=".activities.MainActivity4" />
        <activity
            android:name=".activities.SplachActivity"
            android:theme="@style/Theme.CustomNoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.InscriptionActivity"
            android:theme="@style/Theme.CustomNoBar" />
        <activity
            android:name=".activities.LevelsClass"
            android:theme="@style/Theme.ComeOn" />
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/Theme.ComeOn" />
        <activity android:name=".activities.MainActivity2" />
        <activity
            android:name=".activities.MainActivity3"
            android:parentActivityName=".activities.MainActivity2" />
    </application>

</manifest>